import { ThemeProviderComponent } from "styled-components";
import { Theme } from "./types/Theme";
export declare const withThemesProvider: (themes: Theme[], ThemeProvider?: ThemeProviderComponent<any>) => (story: any) => JSX.Element;
